# avn-test-reports-publisher

Internal CLI to publish Mochawesome HTML reports into `AventusDAO/avn-test-reports` (gh-pages).

## Install (from GitHub)
```bash
npm i -D github:AventusDAO/avn-test-reports-publisher#main
